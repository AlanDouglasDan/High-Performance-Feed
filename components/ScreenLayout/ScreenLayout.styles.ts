import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  flex1: {
    flex: 1,
  },
  header: {
    paddingHorizontal: 16,
    paddingBottom: 16,
  },
  scrollContent: {
    flexGrow: 1,
  },
});
